#!/usr/bin/env bash

[ -f /etc/init.d/tor ] && {
  sudo -u nobody socat "TCP4-LISTEN:${EXTERNAL_TOR_DNS_LOCAL_TCP_PORT},bind=127.0.0.1,reuseaddr,fork" "SOCKS4A:127.0.0.1:${EXTERNAL_TOR_DNS},socksport=${TOR_SOCKS5_PORT}" &
}
